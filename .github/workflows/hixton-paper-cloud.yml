# 2026-09-25: refresh preserved Paper state after exact 0.5.0 strategy authorization.
# 2026-09-20: relaunch after exact preserved-ledger authorization correction.
# 2026-09-20: relaunch with owner-authorized canonical V6 Paper activation.
# 2026-09-20: relaunch after canonical strategy-version source-of-truth repair.
# 2026-09-19: fresh launch for exact owner-authorized Paper V6 activation recovery.
# 2026-09-19: relaunch after public-candle sync was added before controlled activation.
# 2026-09-19: relaunch recovery via data-api.binance.vision public endpoint.
# 2026-09-19: relaunch after common-listing-start Paper activation repair.
name: Hixton Paper 24x7

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/hixton-paper-cloud.yml
  workflow_dispatch:
  schedule:
    # Each runner lease remains active for almost six hours and persists the
    # Paper ledger every 15 minutes. Backup leases are offset by three hours.
    - cron: "7 */6 * * *"

# Paper-only runtime: official Binance public market-data endpoint,
# simulated fills, persistent Paper ledger, no exchange secrets.
permissions:
  contents: write
  actions: read

jobs:
  paper-runtime:
    uses: 127027/Der-Hixton-von-GPT/.github/workflows/hixton-paper-cloud-reusable.yml@gpt/usdc-audit
