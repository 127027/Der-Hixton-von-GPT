name: Hixton Paper Backup Watchdog

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/hixton-paper-watchdog.yml
  workflow_dispatch:
  schedule:
    # Backup lease starts three hours after each primary epoch. The reusable
    # runtime persists every 15 minutes and safely takes over the same ledger.
    - cron: "52 3-23/6 * * *"

permissions:
  contents: write
  actions: read

jobs:
  backup-paper-runtime:
    uses: 127027/Der-Hixton-von-GPT/.github/workflows/hixton-paper-cloud-reusable.yml@gpt/usdc-audit
